---
layout: academy
title: Página no encontrada
permalink: /404.html
---

<main class="cla-main">
<section class="cla-section">
  <div class="cla-container cla-about">
    <h1>Página no encontrada</h1>
    <p>No se ha encontrado la página solicitada. Puedes volver al inicio o acceder a una ruta disponible.</p>
    <div class="cla-lesson-list">
      <a class="cla-lesson-row" href="{{ '/' | relative_url }}"><span>↩</span><strong>Inicio</strong><em>›</em></a>
      {% for item in site.data.routes %}
        {% assign route = item[1] %}
        {% if route.status == 'published' %}
        <a class="cla-lesson-row" href="{{ route.url | relative_url }}"><span>→</span><strong>{{ route.title }}</strong><em>›</em></a>
        {% endif %}
      {% endfor %}
    </div>
  </div>
</section>
</main>
